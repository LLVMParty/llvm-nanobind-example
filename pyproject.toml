[project]
name = "llvm-nanobind-example"
version = "0.1.0"
description = "Super basic llvm-nanobind example project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "llvm-nanobind==21.1.6.1",
]
